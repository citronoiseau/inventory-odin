<div class="filter-container">
  <h3><%= breed.name %></h3>
  <div class="action-container">
    <% if (breed.name !== 'No breed') { %> <% if (breed.admin_created) { %>
    <button
      class="edit-breed-btn-admin"
      data-breed-id="<%= breed.id %>"
      data-breed-name="<%= breed.name %>"
      data-breed-admin="<%= breed.admin_created %>"
    >
      Edit (Admin)
    </button>
    <button
      class="delete-breed-btn-admin"
      data-breed-id="<%= breed.id %>"
      data-breed-name="<%= breed.name %>"
    >
      Delete (Admin)
    </button>
    <% } else { %>
    <button
      class="edit-breed-btn-admin"
      data-breed-id="<%= breed.id %>"
      data-breed-name="<%= breed.name %>"
      data-breed-admin="<%= breed.admin_created %>"
    >
      Edit
    </button>
    <form
      action="/breed/<%= breed.id %>/delete"
      method="POST"
      onsubmit="return confirm('Delete this breed?')"
    >
      <button type="submit">Delete</button>
    </form>
    <% } %> <% } %>
  </div>
</div>
